import React from 'react';

const Dashboard = () => {
  return (
    <div id="dashboard-container" style={{ display: 'block' }}>
      {/* This will be populated by the existing dashboard.html content */}
    </div>
  );
};

export default Dashboard;